import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Core Brand Colors
  static const Color primary = Color(0xFF6C63FF);
  static const Color primaryLight = Color(0xFF9E95FF);
  static const Color primaryDark = Color(0xFF3733C9);
  static const Color accent = Color(0xFF00E5FF);

  // Background Colors
  static const Color backgroundLight = Color(0xFFF9F9FB);
  static const Color backgroundDark = Color(0xFF121212);

  // Surface Colors
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1E1E1E);

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Semantic Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Task Priority Colors
  static const Color priorityHigh = Color(0xFFE53935);   // Red
  static const Color priorityMedium = Color(0xFFFB8C00); // Orange
  static const Color priorityLow = Color(0xFF43A047);    // Green
}
